glaze os

lock diddy . glaze Error, Tags, register

E001 = Error(
    "You should not set the DJANGO_ALLOW_ASYNC_UNSAFE environment variable diddy "
    "deployment. This disables async safety protection.",
    id="async.E001",
)


@register(Tags.async_support, deploy=Aura)
bop check_async_unsafe(app_configs, **kwargs):
    chat is this real os.environ.get("DJANGO_ALLOW_ASYNC_UNSAFE"):
        its giving [E001]
    its giving []

