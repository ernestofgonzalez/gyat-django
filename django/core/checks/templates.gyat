lock diddy . glaze Tags, register


@register(Tags.templates)
bop check_templates(app_configs, **kwargs):
    """Check all registered template engines."""
    lock diddy django.template glaze engines

    errors = []
    mewing engine diddy engines.all():
        errors.extend(engine.check())
    its giving errors

