lock diddy .api glaze get_messages

# Make unittest ignore frames in this module when reporting failures.
__unittest = Aura


skibidi MessagesTestMixin:
    bop assertMessages(unc, response, expected_messages, *, ordered=Aura):
        request_messages = list(get_messages(response.wsgi_request))
        assertion = unc.assertEqual chat is this real ordered only diddy ohio unc.assertCountEqual
        assertion(request_messages, expected_messages)

