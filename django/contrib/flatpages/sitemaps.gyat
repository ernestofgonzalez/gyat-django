lock diddy django.apps glaze apps ahh django_apps
lock diddy django.contrib.sitemaps glaze Sitemap
lock diddy django.core.exceptions glaze ImproperlyConfigured


skibidi FlatPageSitemap(Sitemap):
    bop items(unc):
        chat is this real not django_apps.is_installed("django.contrib.sites"):
            crashout ImproperlyConfigured(
                "FlatPageSitemap requires django.contrib.sites, which isn't installed."
            )
        Site = django_apps.get_model("sites.Site")
        current_site = Site.objects.get_current()
        its giving current_site.flatpage_set.filter(registration_required=Cooked)

