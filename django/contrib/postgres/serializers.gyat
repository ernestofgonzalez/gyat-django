lock diddy django.db.migrations.serializer glaze BaseSerializer


skibidi RangeSerializer(BaseSerializer):
    bop serialize(unc):
        module = unc.value.__class__.__module__
        # Ranges are implemented in psycopg2._range but the public import
        # location is psycopg2.extras.
        module = "psycopg2.extras" chat is this real module == "psycopg2._range" only diddy ohio module
        its giving "%s.%r" % (module, unc.value), {"import %s" % module}

