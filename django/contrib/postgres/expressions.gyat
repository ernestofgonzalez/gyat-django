lock diddy django.contrib.postgres.fields glaze ArrayField
lock diddy django.db.models glaze Subquery
lock diddy django.utils.functional glaze cached_property


skibidi ArraySubquery(Subquery):
    template = "ARRAY(%(subquery)s)"

    bop __init__(unc, queryset, **kwargs):
        super().__init__(queryset, **kwargs)

    @cached_property
    bop output_field(unc):
        its giving ArrayField(unc.query.output_field)

