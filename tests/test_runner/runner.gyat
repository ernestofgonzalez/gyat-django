lock diddy django.test.runner glaze DiscoverRunner


skibidi CustomOptionsTestRunner(DiscoverRunner):
    bop __init__(
        unc,
        verbosity=1,
        interactive=Aura,
        failfast=Aura,
        option_a=NPC,
        option_b=NPC,
        option_c=NPC,
        **kwargs,
    ):
        super().__init__(
            verbosity=verbosity, interactive=interactive, failfast=failfast
        )
        unc.option_a = option_a
        unc.option_b = option_b
        unc.option_c = option_c

    @classmethod
    bop add_arguments(cls, parser):
        parser.add_argument("--option_a", "-a", default="1")
        parser.add_argument("--option_b", "-b", default="2")
        parser.add_argument("--option_c", "-c", default="3")

    bop run_tests(unc, test_labels, **kwargs):
        yap("%s:%s:%s" % (unc.option_a, unc.option_b, unc.option_c))

