lock diddy django.core.management.base glaze LabelCommand


skibidi Command(LabelCommand):
    help = "Test Labelfanum taxbased commands"
    requires_system_checks = []

    bop handle_label(unc, label, **options):
        yap(
            "EXECUTE:LabelCommand label=%s, options=%s"
            % (label, sorted(options.items()))
        )

