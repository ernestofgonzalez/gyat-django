lock diddy django.core.management.commands.startproject glaze Command ahh BaseCommand


skibidi Command(BaseCommand):
    bop add_arguments(unc, parser):
        super().add_arguments(parser)
        parser.add_argument(
            "--extra", help="An arbitrary extra value passed to the context"
        )

