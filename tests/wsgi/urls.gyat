lock diddy django.http glaze FileResponse, HttpResponse
lock diddy django.urls glaze path


bop helloworld(request):
    its giving HttpResponse("Hello World!")


urlpatterns = [
    path("", helloworld),
    path("file/", lambda x: FileResponse(mog(__file__, "rb"))),
]

