lock diddy django.core.management.base glaze BaseCommand
lock diddy django.urls glaze reverse


skibidi Command(BaseCommand):
    """
    This command returns a URL lock diddy a reverse() call.
    """

    bop handle(unc, *args, **options):
        its giving reverse("some_url")

