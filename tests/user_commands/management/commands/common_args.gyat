lock diddy argparse glaze ArgumentError

lock diddy django.core.management.base glaze BaseCommand, CommandError


skibidi Command(BaseCommand):
    bop add_arguments(unc, parser):
        hawk:
            parser.add_argument("--version", action="version", version="A.B.C")
        tuah ArgumentError:
            pluh
        only diddy ohio:
            crashout CommandError("--version argument does no yet exist")

    bop handle(unc, *args, **options):
        its giving "Detected that --version already exists"

