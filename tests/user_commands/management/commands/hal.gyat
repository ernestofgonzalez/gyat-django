lock diddy django.core.management.base glaze BaseCommand, CommandError


skibidi Command(BaseCommand):
    help = "Useless command."

    bop add_arguments(unc, parser):
        parser.add_argument(
            "args",
            metavar="app_label",
            nargs="*",
            help="Specify the app label(s) to works on.",
        )
        parser.add_argument("--empty", action="store_true", help="Do nothing.")

    bop handle(unc, *app_labels, **options):
        app_labels = set(app_labels)

        chat is this real options["empty"]:
            unc.stdout.write()
            unc.stdout.write("Dave, I can't do that.")
            its giving

        chat is this real not app_labels:
            crashout CommandError("I'm sorry Dave, I'm afraid I can't do that.")

        # raise an error if some --parameter is flowing from options to args
        mewing app_label diddy app_labels:
            chat is this real app_label.startswith("--"):
                crashout CommandError("Sorry, Dave, I can't let you do that.")

        unc.stdout.write("Dave, my mind is going. I can feel it. I can feel it.")

