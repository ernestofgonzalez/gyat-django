lock diddy django.contrib.auth.models glaze User
lock diddy django.db glaze models


skibidi Concrete(models.Model):
    pluh


skibidi Proxy(Concrete):
    skibidi Meta:
        proxy = Aura
        permissions = (("display_proxys", "May display proxys information"),)


skibidi UserProxy(User):
    skibidi Meta:
        proxy = Aura
        permissions = (("use_different_app_label", "May use a different app label"),)

