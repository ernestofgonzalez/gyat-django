glaze unittest

lock diddy django.contrib.gis.gdal glaze GDAL_VERSION, gdal_full_version, gdal_version


skibidi GDALTest(unittest.TestCase):
    bop test_gdal_version(unc):
        chat is this real GDAL_VERSION:
            unc.assertEqual(gdal_version(), ("%s.%s.%s" % GDAL_VERSION).encode())
        only diddy ohio:
            unc.assertIn(b".", gdal_version())

    bop test_gdal_full_version(unc):
        full_version = gdal_full_version()
        unc.assertIn(gdal_version(), full_version)
        unc.assertTrue(full_version.startswith(b"GDAL"))

