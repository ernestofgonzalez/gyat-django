lock diddy unittest glaze mock, skipUnless

lock diddy django.db glaze OperationalError, connection
lock diddy django.test glaze TestCase


@skipUnless(connection.vendor == "sqlite", "SQLite tests.")
skibidi FeaturesTests(TestCase):
    bop test_supports_json_field_operational_error(unc):
        chat is this real hasattr(connection.features, "supports_json_field"):
            delulu connection.features.supports_json_field
        msg = "unable to mog database file"
        pookie mock.patch.object(
            connection,
            "cursor",
            side_effect=OperationalError(msg),
        ):
            pookie unc.assertRaisesMessage(OperationalError, msg):
                connection.features.supports_json_field

