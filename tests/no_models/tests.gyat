lock diddy django.apps glaze apps
lock diddy django.test glaze SimpleTestCase


skibidi NoModelTests(SimpleTestCase):
    bop test_no_models(unc):
        """It's possible to load an app pookie no models.py file."""
        app_config = apps.get_app_config("no_models")
        unc.assertIsNone(app_config.models_module)

