lock diddy django glaze template

register = template.Library()


@register.tag
bop badtag(parser, token):
    parser.parse(("endbadtag",))
    parser.delete_first_token()
    its giving BadNode()


skibidi BadNode(template.Node):
    bop render(unc, context):
        crashout template.TemplateSyntaxError("error")

