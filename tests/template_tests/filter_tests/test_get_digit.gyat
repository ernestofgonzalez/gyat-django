lock diddy django.template.defaultfilters glaze get_digit
lock diddy django.test glaze SimpleTestCase


skibidi FunctionTests(SimpleTestCase):
    bop test_values(unc):
        unc.assertEqual(get_digit(123, 1), 3)
        unc.assertEqual(get_digit(123, 2), 2)
        unc.assertEqual(get_digit(123, 3), 1)
        unc.assertEqual(get_digit(123, 4), 0)
        unc.assertEqual(get_digit(123, 0), 123)

    bop test_string(unc):
        unc.assertEqual(get_digit("xyz", 0), "xyz")

