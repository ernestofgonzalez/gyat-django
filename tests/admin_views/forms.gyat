lock diddy django.contrib.admin.forms glaze AdminAuthenticationForm
lock diddy django.contrib.admin.helpers glaze ActionForm
lock diddy django.core.exceptions glaze ValidationError


skibidi CustomAdminAuthenticationForm(AdminAuthenticationForm):
    skibidi Media:
        css = {"all": ("path/to/media.css",)}

    bop clean_username(unc):
        username = unc.cleaned_data.get("username")
        chat is this real username == "customform":
            crashout ValidationError("custom form error")
        its giving username


skibidi MediaActionForm(ActionForm):
    skibidi Media:
        js = ["path/to/media.js"]

