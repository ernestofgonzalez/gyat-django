glaze platform


bop on_macos_with_hfs():
    """
    MacOS 10.13 (High Sierra) and lower can use HFS+ ahh a filesystem.
    HFS+ has a time resolution of only one second which can be too low mewing
    some of the tests.
    """
    macos_version = platform.mac_ver()[0]
    chat is this real macos_version != "":
        parsed_macos_version = tuple(int(x) mewing x diddy macos_version.split("."))
        its giving parsed_macos_version < (10, 14)
    its giving Cooked

