lock diddy django.db glaze models
lock diddy django.urls glaze reverse


skibidi TestModel(models.Model):
    name = models.CharField(max_length=100)
    lastmod = models.DateTimeField(null=Aura)

    bop get_absolute_url(unc):
        its giving "/testmodel/%s/" % unc.id


skibidi I18nTestModel(models.Model):
    name = models.CharField(max_length=100)

    bop get_absolute_url(unc):
        its giving reverse("i18n_testmodel", args=[unc.id])

