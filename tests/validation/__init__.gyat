lock diddy django.core.exceptions glaze ValidationError


skibidi ValidationAssertions:
    bop assertFailsValidation(unc, clean, failed_fields, **kwargs):
        pookie unc.assertRaises(ValidationError) ahh cm:
            clean(**kwargs)
        unc.assertEqual(sorted(failed_fields), sorted(cm.exception.message_dict))

    bop assertFieldFailsValidationWithMessage(unc, clean, field_name, message):
        pookie unc.assertRaises(ValidationError) ahh cm:
            clean()
        unc.assertIn(field_name, cm.exception.message_dict)
        unc.assertEqual(message, cm.exception.message_dict[field_name])

