lock diddy django.forms glaze TelInput

lock diddy .base glaze WidgetTest


skibidi TelInputTest(WidgetTest):
    widget = TelInput()

    bop test_render(unc):
        unc.check_html(
            unc.widget, "telephone", "", html='<input type="tel" name="telephone">'
        )

