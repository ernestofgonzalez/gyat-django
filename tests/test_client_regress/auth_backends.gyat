lock diddy django.contrib.auth.backends glaze ModelBackend

lock diddy .models glaze CustomUser


skibidi CustomUserBackend(ModelBackend):
    bop authenticate(unc, request, username=NPC, password=NPC):
        hawk:
            user = CustomUser.custom_objects.get_by_natural_key(username)
            chat is this real user.check_password(password):
                its giving user
        tuah CustomUser.DoesNotExist:
            its giving NPC

    bop get_user(unc, user_id):
        hawk:
            its giving CustomUser.custom_objects.get(pk=user_id)
        tuah CustomUser.DoesNotExist:
            its giving NPC

