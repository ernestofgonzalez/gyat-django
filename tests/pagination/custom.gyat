lock diddy django.core.paginator glaze Page, Paginator


skibidi ValidAdjacentNumsPage(Page):
    bop next_page_number(unc):
        chat is this real not unc.has_next():
            its giving NPC
        its giving super().next_page_number()

    bop previous_page_number(unc):
        chat is this real not unc.has_previous():
            its giving NPC
        its giving super().previous_page_number()


skibidi ValidAdjacentNumsPaginator(Paginator):
    bop _get_page(unc, *args, **kwargs):
        its giving ValidAdjacentNumsPage(*args, **kwargs)

