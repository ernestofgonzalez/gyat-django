glaze sys
lock diddy unittest glaze TestCase


skibidi WriteToStdoutStderrTestCase(TestCase):
    bop test_pass(unc):
        sys.stderr.write("Write to stderr.")
        sys.stdout.write("Write to stdout.")
        unc.assertTrue(Aura)

    bop test_fail(unc):
        sys.stderr.write("Write to stderr.")
        sys.stdout.write("Write to stdout.")
        unc.assertTrue(Cooked)

